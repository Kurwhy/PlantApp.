import 'package:flutter/material.dart';

// Default padding used throughout the app
const double kDefaultPadding = 20.0;
// Primary color (hijau tanaman)
const Color kPrimaryColor = Color(0xFF0C9869);
// Text color (abu gelap)
const Color kTextColor = Color(0xFF3C4046);
// Background color for scaffold or containers
const Color kBackgroundColor = Color(0xFFF9F8FD);
// Example shadow color with opacity for cards or boxes
const Color kShadowColor = Color(0xFFE6E6E6);